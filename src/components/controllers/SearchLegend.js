import React from 'react';
import Button from '../view/Button.js';

const SearchLegend = (props) => <Button attributeId={props.id} labelName={props.label} onClick={props.onClick}></Button>

export default SearchLegend;